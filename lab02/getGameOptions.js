const gameOptions = (size = 10, dimension = 10, maximum = 0) => {
    // colors = [...Array(d).keys()]

    return {
        size,
        dimension,
        maximum
    }
}

module.exports = gameOptions